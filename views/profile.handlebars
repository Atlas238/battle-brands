<div>{{> navbar}}</div>
<h1 class="display-3 text-center mt-5 mb-5"> Welcome back, {{username}}!</h1>

<h4 class="display-5">Your Creatures: </h4>
<div id="profileCards" class="d-flex flex-wrap flex-md-row flex-sm-column container">

    {{#each userCreatures}}
      {{> brand-card}}
    {{/each}}

</div>